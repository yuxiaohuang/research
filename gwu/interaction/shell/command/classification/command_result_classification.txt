
module load python

count=0;

NR_procs=6;

for myfile in /home/yuxiaohuang/research/gwu/interaction/script/classification/*.txt;         
    do (sh $myfile) &
    let count+=1
    [[ $((count % NR_procs)) -eq 0 ]] && wait
done
