
module load python

count=0;

NR_procs=10;

for myfile in /home/yuxiaohuang/research/gwu/interaction_nontemporal/script/*.txt;         
    do (sh $myfile) &
    let count+=1
    [[ $((count % NR_procs)) -eq 0 ]] && wait
done
